# Cypress E2E Tests

This directory contains Cypress end-to-end tests.

## Run

- `npm run e2e` for headless run
- `npm run e2e:open` for interactive runner
- `npm run e2e:headed` for headed browser run

## Notes

- Tests run directly against the app via UI flows.
- No fixture or mock harness is used in this suite.
